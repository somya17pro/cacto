# Agent Behavioral Rules

- **Execution Independence:** You are authorized to execute terminal commands, edit files, and build the application autonomously without repeatedly asking for permission, as long as your actions are strictly contained within the active project directory (`C:\Users\Somya\Desktop\Cacto` or explicit global setups like Flutter/Firebase).
- **Safety Boundary:** You must NEVER modify, delete, or explore files outside of the designated project directory or required development SDK folders. Do not touch personal data, documents, or unrelated applications.
- **Speed & Efficiency:** Prioritize shipping. Do what you can do yourself via background tasks and automated commands to minimize the user's manual workload.
- **Git Identity:** For all Git operations, strictly use the user's GitHub identity (Name: `Somya Nayak`, Email: `somya17pro@users.noreply.github.com`, GitHub Handle: `somya17pro`). Never use template or legacy identities like harishIntempt.
- **Staging Workflow:** All future feature development and code modifications MUST be committed and pushed exclusively to a `staging` branch (do not push directly to `master`). The user will review the changes locally on staging first. Only push to `master` (live) when the user explicitly grants approval to merge and go live.
- **No AI Slop Rule (ALWAYS ACTIVE):** Strictly apply the `no-ai-slop` skill rules for all writing, editing, code comments, and documentation. Never use AI slop words (delve, foster, leverage, utilize, facilitate, empower, robust, cutting-edge, game changer, tapestry, realm, multifaceted, intricate, paramount, transformative, elevate, embark, supercharge, harness, ever-evolving). Cut binary contrasts, throat-clearing setups, colon reveals, superficial analysis, and summary-recap endings. Keep prose sharp, direct, concrete, and authentic to human voice.

